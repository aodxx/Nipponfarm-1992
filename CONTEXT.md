# CONTEXT — เริ่มอ่านที่นี่

เอกสารนี้สำหรับใครก็ตาม (คนหรือ AI agent) ที่มาต่องานบนโปรเจกต์นี้ อ่านไฟล์นี้ก่อนเริ่มแก้โค้ด

## โปรเจกต์นี้คืออะไร

Nipponfarm — ระบบจัดการฟาร์มหมูครบวงจร รายละเอียดฟีเจอร์ทั้งหมด → `PRD.md`

## สถานะปัจจุบัน (อัปเดตล่าสุด: ดู CHANGELOG.md)

- **เฟส 1 (โครงข้อมูล + Overview):** ใช้งานได้จริงแล้ว — มีหน้า login (`pages/login.html`), Overview ดึงข้อมูลจริงจาก Supabase (ไม่ใช้ mock data แล้ว) ต้อง login ก่อนถึงจะเข้าหน้า Overview ได้
- **Supabase:** โปรเจกต์ "nipponfarm" สร้างแล้วและรัน migrations ครบ (`0001`, `0002` RLS, `0003` trigger) + seed แล้ว ทดสอบ RLS ผ่านครบ 3 มุมมอง (anonymous/staff/admin) — URL/anon key ใส่ใน `js/supabase-client.js` แล้ว บัญชีผู้ใช้สร้างผ่าน Supabase Dashboard เท่านั้น (ไม่มีสมัครสมาชิกสาธารณะ) — ดู API.md ข้อ 4
- **เฟส 2-5:** ยังไม่เริ่ม — ดูแผนใน `PRD.md`, มี GitHub Issues แยกตามเฟสแล้ว

## ต้องอ่านอะไรก่อนแก้โค้ด

| ต้องการทำอะไร | อ่านไฟล์นี้ก่อน |
|---|---|
| เพิ่ม/แก้ฟีเจอร์ | `PRD.md` — เช็คว่าอยู่ในขอบเขตเฟสไหน |
| แก้/เพิ่มตารางฐานข้อมูล | `DATABASE.md` และ `supabase/migrations/` (ห้ามแก้ migration เก่าที่ deploy แล้ว — เพิ่มไฟล์ migration ใหม่เสมอ) |
| เพิ่มหน้าจอใหม่ | `ARCHITECTURE.md` ข้อ 2-3 (โครงสร้างโฟลเดอร์ + data layer pattern) |
| ต่อ Supabase (query/Edge Function) | `API.md` |
| จะเปลี่ยนแนวทางที่เคยตัดสินใจไว้ | อ่าน `DECISIONS.md` ก่อน แล้วเพิ่ม ADR ใหม่ ไม่ลบของเดิม |
| แนวทางเขียนโค้ด/ข้อตกลงร่วม | `AGENTS.md` |

## กติกาสั้นๆ ที่ต้องจำ

1. ทุกหน้าใช้ `css/style.css` เดียวกัน ห้ามสร้างธีมแยก
2. ทุกการเชื่อม Supabase ผ่าน `js/supabase-client.js` จุดเดียว
3. แก้อะไรที่กระทบสถาปัตยกรรมหรือ schema → เพิ่มบันทึกใน `DECISIONS.md` และ/หรือ `CHANGELOG.md`
4. UI/ข้อความทั้งหมดเป็นภาษาไทย โค้ด/ชื่อตัวแปรเป็นภาษาอังกฤษ
