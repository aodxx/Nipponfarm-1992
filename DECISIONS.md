# DECISIONS — Nipponfarm

Log การตัดสินใจสำคัญ เรียงตามเวลา แต่ละรายการคือเหตุผลที่ยืนอยู่เบื้องหลังโครงสร้างปัจจุบัน — ถ้าจะเปลี่ยนแนวทางใดที่มีในนี้ ให้เพิ่มรายการใหม่ (ไม่ลบของเดิม) พร้อมเหตุผลที่เปลี่ยน

## ADR-001: ใช้ Supabase (Postgres) แทน Google Sheets/Apps Script

**บริบท:** สแต็กที่ใช้ประจำคือ Google Sheets + Apps Script + GitHub Pages
**ตัดสินใจ:** Nipponfarm ใช้ Supabase แทน
**เหตุผล:** ข้อมูลมีความสัมพันธ์ซับซ้อนหลายชั้น (แม่พันธุ์ ↔ ประวัติ ↔ คอก ↔ งาน ↔ การเงิน ↔ พนักงาน) และต้องการสิทธิ์แยกตาม role (staff เห็นเฉพาะของตัวเอง) ซึ่ง Sheets ทำได้ยากเมื่อข้อมูลโตขึ้น ส่วน Supabase มี RLS และ relational schema ให้ตรงตัว

## ADR-002: Static frontend (HTML/CSS/JS) เรียก Supabase ตรงจาก client ไม่มี backend server แยก

**ตัดสินใจ:** ไม่เขียน backend server ของตัวเอง ใช้ `supabase-js` เรียกตรงจากเบราว์เซอร์ โฮสต์ frontend บน GitHub Pages เหมือนโปรเจกต์อื่นที่ผ่านมา
**เหตุผล:** ลดความซับซ้อนด้าน ops (ไม่ต้องดูแล server แยก) สิทธิ์การเข้าถึงข้อมูลบังคับที่ระดับฐานข้อมูล (RLS) อยู่แล้ว จึงปลอดภัยพอสำหรับเคสนี้
**ข้อยกเว้น:** งานที่ต้องปิดบัง secret (เช่น Gemini API key สำหรับอ่านบิล) ใช้ Supabase Edge Function แทน — ดู ADR-005

## ADR-003: ทิศทางดีไซน์ — "สมุดบันทึกฟาร์ม" ไม่ใช่ SaaS dashboard ทั่วไป

**ตัดสินใจ:** ใช้ layout แบบแถว/เส้นคั่น (ledger/logbook) แทนการ์ดลอยพร้อมเงา, สถานะสื่อผ่านแถบสีด้านซ้าย, โทนสี greige/เขียวพืชไร่/น้ำตาลทอง/แดงสนิม, ฟอนต์ Taviraj (หัวข้อ) + Sarabun (เนื้อหา)
**เหตุผล:** ผู้ใช้เป็นพนักงานฟาร์มเช็คข้อมูลรวดเร็วในสภาพแวดล้อมทำงานจริง ไม่ใช่ผู้ใช้ SaaS ทั่วไป ต้องอ่านง่าย ไม่รกด้วยองค์ประกอบตกแต่งที่ไม่จำเป็น
**อ้างอิง:** โทเคนสี/ฟอนต์ทั้งหมดกำหนดไว้ที่ `css/style.css` — ทุกหน้าจอใหม่ต้องใช้ไฟล์เดียวกันนี้ ห้ามสร้างธีมแยก

## ADR-004: ลำดับการพัฒนาเป็น 5 เฟส

**ตัดสินใจ:** (1) โครงข้อมูล+Overview → (2) แม่พันธุ์/ปฏิทิน → (3) ผังคอก → (4) การเงิน → (5) พนักงาน/เงินเดือน
**เหตุผล:** เฟส 1-2 เป็นแกนหลักที่ฟีเจอร์อื่นอ้างอิงถึง (pigs, tasks) จึงทำก่อน เฟส 4-5 เป็นข้อมูลการเงิน/บุคคลที่ต้องการ RLS รัดกุมกว่า จึงทำหลังเมื่อโครง auth/role นิ่งแล้ว

## ADR-005: อ่านบิลด้วย AI ผ่าน Edge Function ไม่เรียกจาก client ตรงๆ

**ตัดสินใจ:** Gemini API call อยู่ใน Supabase Edge Function (`read-bill`) เท่านั้น
**เหตุผล:** ป้องกัน API key หลุดในโค้ดฝั่งเบราว์เซอร์ที่ทุกคนเปิดดูได้ (สอดคล้องกับแพทเทิร์นที่เคยใช้ Gemini ผ่าน Apps Script ในโปรเจกต์อื่น)

## ADR-006: ประวัติหมู (ผสม/ท้อง/คลอด/หย่านม/รักษา) เก็บในตารางเดียว `pig_events` แทนแยกตารางย่อย

**ตัดสินใจ:** ใช้ตาราง log เดียว มีคอลัมน์ `event_type` แยกประเภท แทนการสร้าง `breeding_records`, `pregnancy_checks`, `farrowing_records`, `weaning_records` แยกกัน
**เหตุผล:** เหตุการณ์ทั้งหมดมีรูปร่างข้อมูลคล้ายกัน (หมูตัวไหน, วันที่, บันทึกโดยใคร, หมายเหตุ) และเป็น "ไทม์ไลน์" เดียวกันของหมูตัวนั้น query ประวัติทั้งหมดของหมูตัวหนึ่งด้วย query เดียว (`where pig_id = ...`) ง่ายกว่าการ join หลายตาราง ก็เพียงพอต่อความต้องการใช้งานจริงในทุกเฟส

## ADR-007: repo เดียว ไม่มี build step

**ตัดสินใจ:** ทุกไฟล์เป็น HTML/CSS/JS ธรรมดา ไม่ใช้ bundler/framework (React, Vue ฯลฯ)
**เหตุผล:** ตรงกับแพทเทิร์นโปรเจกต์อื่นที่ใช้ GitHub Pages ตรงๆ, ลด dependency, เปิดไฟล์แก้ได้ทันทีโดยไม่ต้อง build

## ADR-008: แยก schema เป็น Supabase migrations แทนไฟล์ SQL รวมไฟล์เดียว

**บริบท:** `docs/schema.sql` เดิมรวมทุกเฟสไว้ไฟล์เดียว ไม่มีลำดับ ไม่มี RLS รันซ้ำใน environment อื่นจะชนชื่อตาราง และแก้ schema ภายหลังไม่มีประวัติชัดเจน (พบจาก code review)
**ตัดสินใจ:** ย้ายเป็น `supabase/migrations/000X_*.sql` แยกไฟล์ตามลำดับที่ต้องรัน (`0001_initial_schema.sql` → `0002_enable_rls.sql` → `0003_triggers.sql`) พร้อม `supabase/seed.sql` แยกต่างหาก ลบ `docs/schema.sql` ไฟล์เดิมทิ้ง
**เหตุผล:** รันซ้ำได้ในทุก environment ด้วยลำดับเดียวกัน, มีประวัติการเปลี่ยนแปลงชัดเจนทีละไฟล์, และบังคับให้ RLS ถูกเปิดเป็นขั้นตอนที่แยกออกมาให้เห็นชัด ไม่ใช่ "จะเพิ่มทีหลัง" ที่อาจถูกลืม

## ADR-009: ต้องเปิด RLS + policy จริงให้ครบทุกตาราง ก่อนต่อ client เข้า Supabase ได้

**บริบท:** เอกสารเดิมอธิบายว่าระบบพึ่ง RLS เป็นชั้นบังคับสิทธิ์ แต่ schema ที่มีจริงยังไม่มี policy ใดๆ ถ้าต่อ client ตอนนั้นข้อมูลฟาร์ม/การเงิน/พนักงานจะเปิดอ่าน-เขียนได้ไม่จำกัดสิทธิ์ (พบจาก code review — ความเสี่ยงระดับ P0)
**ตัดสินใจ:** `0002_enable_rls.sql` ต้องรันสำเร็จครบทุกตาราง (พร้อม helper `is_admin()`) ก่อนใส่ URL/anon key ใน `js/supabase-client.js` เป็นขั้นตอนบังคับ ไม่ใช่ทางเลือก
**เหตุผล:** ป้องกันข้อมูลรั่ว/ถูกแก้ไขเกินสิทธิ์ในช่วงพัฒนา — RLS ต้องมาก่อนการเชื่อมต่อจริงเสมอ ไม่ใช่ "ทำทีหลังได้"

## ADR-010: สถานะงาน/เหตุการณ์ต้องสื่อด้วยข้อความเสมอ ไม่ใช่สีเพียงอย่างเดียว

**บริบท:** ดีไซน์เดิม (ADR-003) ใช้แถบสีซ้าย + สีข้อความบอกสถานะ (ปกติ/ใกล้ครบกำหนด/เลยกำหนด) แต่ผู้ใช้ที่แยกสียาก (color blindness) จะเสียข้อมูลสำคัญ เพราะบางป้าย (เช่น "วันนี้") ไม่ได้บอกระดับความเร่งด่วนเป็นคำ (พบจาก code review)
**ตัดสินใจ:** ทุกแถวแสดงคำสถานะเป็นข้อความเสมอ ("ปกติ · อีก N วัน", "ใกล้ครบกำหนด · วันนี้", "เลยกำหนด N วัน") และใส่ `aria-label` สรุปสถานะเต็มบนแต่ละแถวสำหรับ screen reader สีใช้เป็นตัวเสริมความเร็วในการสแกนสายตาเท่านั้น ไม่ใช่ช่องทางเดียว

## ADR-011: เพิ่มไอคอน PWA แล้ว แต่ยังไม่ทำ service worker/offline ในตอนนี้

**บริบท:** manifest อ้างไฟล์ไอคอนที่ยังไม่มีจริง (404) ทำให้ดูเหมือน PWA พร้อมใช้งานทั้งที่ยังไม่มี service worker (พบจาก code review)
**ตัดสินใจ:** สร้างไอคอนจริง (`icons/icon-192.png`, `icons/icon-512.png`) แก้ 404 ทันที แต่ **ยังไม่เพิ่ม service worker** — ระบบยังต้องต่ออินเทอร์เน็ตเสมอ ไม่รองรับ offline ในตอนนี้
**เหตุผล:** manifest + ไอคอนให้ประโยชน์ "ติดตั้งเป็นแอปได้" (installable/standalone) แม้ไม่มี offline การเพิ่ม service worker เป็นงานที่ควรวางแผนแยกต่างหาก (cache strategy, ข้อมูลที่ต้อง sync) ไม่ใช่แค่เพิ่มไฟล์เฉยๆ — ติดตามเป็นงานในอนาคต ไม่ผูกกับเฟส 1-5

## ADR-012: pen ของ task ดึงผ่าน pigs เสมอ ไม่เก็บ pen_id ซ้ำในตาราง tasks

**บริบท:** โค้ดตัวอย่างใน API.md เคย query `pens(code)` ตรงจาก `tasks` ซึ่งใช้ไม่ได้จริงเพราะ `tasks` ไม่มี foreign key ไป `pens` ตรงๆ มีแค่ `pig_id` (พบจาก code review)
**ตัดสินใจ:** ไม่เพิ่มคอลัมน์ `pen_id` ซ้ำใน `tasks` — คอกของงานที่ผูกกับหมูตัวใดตัวหนึ่ง ต้อง join ผ่าน `pigs.pen_id` เสมอ (`pigs(pens(code))`) ส่วนงานทั่วไปที่ไม่ผูกกับหมู (เช่น ซ่อมบำรุง) ใช้ `title`/`notes` อธิบายสถานที่แทน
**เหตุผล:** ถ้าเก็บ `pen_id` ซ้ำในทั้ง `pigs` และ `tasks` เมื่อย้ายหมูไปคอกอื่น ข้อมูลใน `tasks` เก่าจะไม่ sync ตาม (stale data) การ join ผ่าน `pigs` เพียงจุดเดียวรับประกันว่าคอกที่แสดงตรงกับความจริงเสมอ

## ADR-013: ต้องมีหน้า Login ก่อน ถึงจะสลับหน้าจอใดๆ จาก mock data ไปใช้ Supabase จริงได้

**บริบท:** หลังตั้งค่า RLS จริง (ADR-009) เกือบทุกตารางใช้ policy `auth.role() = 'authenticated'` ซึ่งหมายความว่าถ้าไม่มี session login การ query จาก client จะได้แถวว่างเสมอ (ไม่ error แค่ไม่มีข้อมูล) การสลับหน้า Overview ไปใช้ query จริงตอนนี้จะทำให้หน้าจอว่างเปล่าทั้งที่ข้อมูลมีอยู่จริงใน DB
**ตัดสินใจ:** ยังไม่สลับ `getOverviewData()` ในเฟส 1 ไปใช้ query จริง จนกว่าจะมีหน้า login (Supabase Auth) — เพิ่มเป็นงานเดี่ยวที่ต้องทำก่อนเชื่อมหน้าจอไหนก็ตามเข้ากับข้อมูลจริง ไม่ผูกกับเฟสใดเฟสหนึ่งใน PRD.md เดิม
**เหตุผล:** ป้องกันไม่ให้ทีมเข้าใจผิดว่า "เชื่อม Supabase แล้ว" หมายถึงหน้าจอจะใช้งานได้จริงทันที ทั้งที่ยังขาดชิ้นส่วน auth ที่จำเป็นต่อ RLS ทุกจุด

## ADR-014: Login แบบ email/password เท่านั้น ไม่มีหน้าสมัครสมาชิกสาธารณะ

**บริบท:** ต้องปิดช่องว่างจาก ADR-013 — สร้างหน้า login จริงก่อนสลับ Overview ไปใช้ Supabase
**ตัดสินใจ:** ใช้ Supabase Auth email/password (`pages/login.html`) ไม่เปิดหน้าสมัครสมาชิกสาธารณะ บัญชีทั้งหมด (staff/admin) ต้องให้แอดมินสร้างเองผ่าน Supabase Dashboard แล้ว insert แถวคู่กันในตาราง `staff`
**เหตุผล:** เป็นระบบภายในฟาร์ม ผู้ใช้มีจำนวนจำกัดและรู้จักตัวตนชัดเจน การเปิดสมัครสมาชิกสาธารณะจะสร้างความเสี่ยงที่ไม่จำเป็น (ผู้ใช้ signup เองไม่มีแถว `staff` คู่กันจะเข้าระบบไม่ได้อยู่ดี เพราะ RLS ตรวจสอบ `staff.role`) — งานสร้างบัญชีจริงจึงเป็นขั้นตอนแมนนวลของแอดมินตั้งแต่ต้น
