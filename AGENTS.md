# AGENTS — แนวทางสำหรับ AI agent ที่มาช่วยเขียนโค้ดในโปรเจกต์นี้

## ก่อนเริ่มงานทุกครั้ง

1. อ่าน `CONTEXT.md` ก่อนเสมอ เพื่อรู้สถานะปัจจุบัน
2. เช็คว่างานที่จะทำอยู่ในขอบเขตเฟสไหนตาม `PRD.md`
3. ถ้าเป็นเรื่องฐานข้อมูล เช็ค `DATABASE.md`/`supabase/migrations/` ว่ามีตารางออกแบบไว้แล้วหรือยัง — ถ้ามีแล้วให้ใช้ตามนั้น อย่าออกแบบใหม่ซ้ำ **ห้ามแก้ไฟล์ migration ที่มีอยู่แล้ว** ถ้าต้องเปลี่ยน schema ให้เพิ่มไฟล์ migration ใหม่ (เช่น `0004_*.sql`) เสมอ

## ข้อตกลงการเขียนโค้ด

- **ภาษา:** UI/ข้อความ/comment อธิบายเป็นภาษาไทย, ชื่อตัวแปร/ฟังก์ชัน/ตาราง/คอลัมน์เป็นภาษาอังกฤษ snake_case (DB) หรือ camelCase (JS)
- **ไม่มี build step:** เขียน HTML/CSS/JS ธรรมดา ไม่เพิ่ม bundler, framework, หรือ npm dependency ใหม่โดยไม่จำเป็น (ดู ADR-007 ใน `DECISIONS.md`)
- **ธีม:** ใช้ CSS variables ที่กำหนดไว้ใน `css/style.css` (สี/ฟอนต์) เท่านั้น ห้าม hardcode สีใหม่ในไฟล์อื่น ถ้าต้องการสีเพิ่ม ให้เพิ่มเป็น variable ใน `:root` ของ `style.css`
- **Data layer pattern:** ทุกหน้าจอมีฟังก์ชัน `get<ชื่อหน้า>Data()` เป็นจุดเดียวที่ดึงข้อมูลจาก Supabase จริง แยกจากฟังก์ชัน render เสมอ — ดูตัวอย่างใน `js/app.js`
- **Auth guard:** ทุกหน้าที่ต้อง login เรียก `requireAuth()` จาก `js/auth-guard.js` เป็นบรรทัดแรกใน init เสมอ
- **ฟังก์ชันร่วม:** สิ่งที่ใช้มากกว่า 1 หน้า (DOM helpers, คำนวณวันที่/สถานะ, loading/error state) ต้องอยู่ใน `js/ui-helpers.js` ไม่ก็อปโค้ดซ้ำไปไว้ไฟล์เฉพาะหน้า ก่อน render อะไรทั้งสิ้น
- **Supabase client:** สร้างที่ `js/supabase-client.js` ที่เดียว ห้ามสร้างซ้ำในไฟล์อื่น
- **Mock data:** ต้องมีรูปร่าง (field names) ตรงกับตารางจริงใน `DATABASE.md` เป๊ะ เพื่อสลับไป Supabase จริงได้โดยไม่ต้องแก้โค้อ render

## เมื่อทำการตัดสินใจที่กระทบสถาปัตยกรรม

ถ้าเปลี่ยน/เพิ่มการตัดสินใจระดับสถาปัตยกรรม (เช่น เปลี่ยน pattern, เพิ่มตารางใหม่นอกแผนเดิม, เปลี่ยนวิธี auth) ให้:
1. เพิ่ม ADR ใหม่ใน `DECISIONS.md` (ไม่ลบของเดิม)
2. อัปเดต `DATABASE.md`/`ARCHITECTURE.md`/`API.md` ให้ตรงกับของจริงเสมอ (ห้ามให้เอกสารกับโค้ดไม่ตรงกัน)
3. เพิ่มรายการใน `CHANGELOG.md`

## Commit message

ภาษาอังกฤษสั้นๆ บอกว่าทำอะไร เช่น `Add pigs.html: pig list + detail view (phase 2)` — ถ้า commit กระทบ schema ให้ระบุ `(schema)` ต่อท้ายด้วย

## สิ่งที่ห้ามทำ

- ห้ามใส่ API key/secret ลงในโค้ดฝั่ง client (ไฟล์ใน `js/`, `index.html`, `pages/*.html`) — ของที่ต้องปิดบัง (เช่น Gemini key) ต้องอยู่ใน Edge Function เท่านั้น (ดู `API.md`)
- ห้ามสร้างตารางใหม่โดยไม่อัปเดต `DATABASE.md` ให้ตรงกัน
- ห้ามเปลี่ยนโทนสี/ฟอนต์หลักโดยไม่มีเหตุผลบันทึกใน `DECISIONS.md`
- ห้ามใช้ `innerHTML` กับข้อมูลที่มาจากฐานข้อมูล/ผู้ใช้ — สร้าง DOM node แล้วใส่ค่าเป็น `textContent` เท่านั้น (ป้องกัน DOM XSS)
- ห้ามเปิดตารางใหม่ให้ client เรียกใช้ก่อนเขียน RLS policy ให้ตารางนั้นใน `supabase/migrations/` (ดู ADR-008/009 ใน `DECISIONS.md`)
